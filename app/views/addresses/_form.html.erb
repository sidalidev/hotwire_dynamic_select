<%= form_with(model: address) do |form| %>
  <% if address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(address.errors.count, "error") %> prohibited this address from being saved:</h2>

      <ul>
        <% address.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div data-controller='country'>
    <div class="field">
      <%= form.label :country %>
      <%= form.select :country, CS.countries.invert, { prompt: 'Select a country' }, data: { action: 'change->country#change' } %>
    </div>

    <div class="field">
      <%= form.label :state %>
      <%= form.select :state, [], {}, data: { country_target: 'stateSelect' } %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
